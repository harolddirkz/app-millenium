spring:
  application:
    name: demoCours
  datasource:
    url: jdbc:mysql://localhost:3306/management_courses
    username: root
    password: admin
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: true
    hibernate.ddl-auto: update
logging:
  level:
    org.hibernate.SQL: DEBUG

##spring.application.name=demoCours